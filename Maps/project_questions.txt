Список вопросов для подготовки к защите проекта:

== Файл: components/Map.tsx ==

1. Какую основную библиотеку вы используете для отображения карты и для чего она нужна?
2. Что делает строка `router.push('/marker/' + marker.id)`? Как Expo Router понимает, какой экран открыть по этому адресу (что особенного в имени файла `[id].tsx`)?
3. В чем фундаментальные проблемы использования `Math.random()` для генерации ID? Какой подход был бы надежнее?
4. Почему для обновления состояния (добавления маркера) используется `setMarkers([...markers, newMarker])`, а не `markers.push(newMarker)`? (Вопрос об иммутабельности).
5. Зачем нужна переменная состояния `isMapReady`? Что может пойти не так, если рендерить маркеры до того, как карта будет готова?

== Файл: app/marker/[id].tsx ==

6. В чем разница между хуками `useLocalSearchParams()` и `useRouter()` в этом файле?
7. Что делает строка `const marker = markers.find(...)`? Откуда берутся `markers` и `id` для сравнения?
8. Объясните по шагам логику обновления в функции `handleAddImage`, особенно блок с `.map()`. Почему используется `.map()`, а не другой метод?

== Файл: app/context/MarkerContext.tsx ==

9. Что такое `children` в React? Какова роль компонента `MarkerProvider` как "обертки" для всего приложения?
10. Зачем нужен кастомный хук `useMarkers()`? Почему не использовать `useContext(MarkerContext)` напрямую в компонентах?

== Итоговые блиц-вопросы ==

11. Какой вариант вы выберете для обновления state-массива, чтобы React перерисовал компонент: `newArray.push(newItem)` или `[...array, newItem]`?
12. Если создать файл `app/about.tsx`, по какому URL он будет доступен?
13. Какую одну строчку нужно написать, чтобы получить `markers` и `setMarkers` из контекста внутри компонента?
14. Какой метод массива (`.find()` или `.map()`) нужно использовать, чтобы найти ОДИН элемент в массиве по условию?
